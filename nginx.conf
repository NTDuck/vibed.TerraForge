server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # static mockup: cache-busted by rebuild, so revalidate always
    location / {
        add_header Cache-Control "no-store";
        try_files $uri $uri/ /index.html;
    }
}
